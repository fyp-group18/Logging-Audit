# Pinned to the versions in the environment that produced the reported results
# (Python 3.14, uv-managed). `uv pip freeze` is the source for every version below.

# Backend API Framework
fastapi==0.136.3
uvicorn==0.48.0
python-multipart==0.0.29

# Core AI & Graph Dependencies
google-genai==1.75.0
langchain-google-genai==4.2.3
langchain-core==1.4.0
langgraph==1.2.1

# Database & Vector Storage (Postgres)
sqlalchemy==2.0.49
pgvector==0.4.2
langgraph-checkpoint-postgres==3.1.0
psycopg-pool==3.3.1
psycopg[binary]==3.3.4

# Data Validation & Utilities
pydantic==2.13.4
tenacity==9.1.4
python-dotenv==1.2.2
numpy==2.4.6
msgpack==1.1.2

# Evaluation & Analysis
scipy==1.17.1
httpx==0.28.1

# Auth (eval API dependencies)
passlib[argon2]==1.7.4
python-jose[cryptography]==3.5.0

# Google Cloud (eval worker)
google-cloud-storage==3.10.1
google-auth==2.53.0

# Testing
pytest==9.0.3
